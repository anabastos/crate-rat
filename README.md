# tidal-crate
for my crate digger fellas

tidal api
tidal dl

- set crates
    - path
    - ssh

- add playlist to crates

- sync
    - songs on a playlists will be sync between all AVAILABLE(sometimes its an external hd or pendrive) crates
    - will download only the unsync songs

- can add tags or comments to playlists and songs

- can search by tags, artists, track name, playlist name

- can import a spotify playlist <- scrapping?

- should work on windows and linux